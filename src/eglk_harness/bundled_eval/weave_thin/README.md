# weave_thin

Thin Weave-shaped tasks for eglk. Full LH harness lives in `../vendor/` after `fetch_lh_eval.sh`.

Map LH task → eglk: write `.goal.md` via `eglk-harness eval --suite weave_thin`, run harness, score with `check` block offline only.
