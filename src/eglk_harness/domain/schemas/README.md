# Packaged JSON Schema copies (shipped with eglk-harness).

Canonical projection/event schemas align with `design/kernel/schemas/README.md`.
Legacy aliases (`claim` → `action_claim`, `state` → `run_projection`, etc.) are resolved in `kernel/schema_validate.py`.
